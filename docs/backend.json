
{
  "entities": {
    "NewsletterSubscription": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "NewsletterSubscription",
      "type": "object",
      "description": "Represents a user's subscription to the restaurant's newsletter, capturing their contact information for updates and promotions.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The full name of the user subscribing to the newsletter."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user subscribing to the newsletter.",
          "format": "email"
        },
        "subscriptionDate": {
          "type": "string",
          "description": "The date and time when the user subscribed to the newsletter.",
          "format": "date-time"
        }
      },
      "required": [
        "name",
        "email",
        "subscriptionDate"
      ]
    },
    "ComingSoonLead": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ComingSoonLead",
      "type": "object",
      "description": "Represents a lead captured from the 'Coming Soon' page, including their name, email, WhatsApp, and interest rating.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The full name of the interested lead."
        },
        "email": {
          "type": "string",
          "description": "The email address of the lead.",
          "format": "email"
        },
        "whatsapp": {
          "type": "string",
          "description": "The WhatsApp number of the lead."
        },
        "rating": {
          "type": "number",
          "description": "A rating from 1 to 5 stars indicating the user's interest or experience.",
          "minimum": 1,
          "maximum": 5
        },
        "submissionDate": {
          "type": "string",
          "description": "The date and time when the lead was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "name",
        "email",
        "submissionDate"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/newsletter_subscriptions/{subscriptionId}",
        "definition": {
          "entityName": "NewsletterSubscription",
          "schema": {
            "$ref": "#/backend/entities/NewsletterSubscription"
          },
          "description": "Stores individual newsletter subscriptions.",
          "params": [
            {
              "name": "subscriptionId",
              "description": "The unique identifier for a specific newsletter subscription document."
            }
          ]
        }
      },
      {
        "path": "/coming_soon_leads/{leadId}",
        "definition": {
          "entityName": "ComingSoonLead",
          "schema": {
            "$ref": "#/backend/entities/ComingSoonLead"
          },
          "description": "Stores leads captured from the site with optional rating.",
          "params": [
            {
              "name": "leadId",
              "description": "The unique identifier for a specific lead document."
            }
          ]
        }
      }
    ],
    "reasoning": "Estrutura simplificada para captura de leads e assinaturas de newsletter com suporte a classificação."
  }
}
